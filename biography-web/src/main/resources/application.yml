archive:
  path: /path/to/archive/
  
import:
  path: /path/to/import/
  
solr:
  index:
    url: http://solr.url
    
spring:
  resources:
    static-locations: classpath:/static/
  http:
    multipart:
      max-file-size: 128MB
      max-request-size: 128MB
