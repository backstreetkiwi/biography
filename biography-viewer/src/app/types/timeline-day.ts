export class TimelineDay {
    
    year: number;
    month: number;
    day: number;
    count: number;

    constructor(year: number, month: number, day: number, count: number) {
        this.year = year;
        this.month = month;
        this.day = day;
        this.count = count;
    }
}